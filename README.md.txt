# 🧠 Spam Message Detector (Flask App)

This is a simple web application built using Flask that detects whether a given message is **Spam** or **Not Spam** using a machine learning model trained on SMS data.

---

## 🚀 Features

- Input a text message and get an instant prediction.
- Pre-trained ML model for spam detection using NLP.
- Simple web UI using HTML templates.

---

## 🛠 Requirements

Install the following Python packages:

```bash
pip install -r requirements.txt
